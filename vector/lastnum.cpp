#include <iostream>
#include<vector>

using namespace std;

int main() {
    int n=5;
    vector<int>vec(5);
    for(int i=0;i<=5;i++){
        cin>>(vec[i]);
cout<<("Enter the value of array"); 
    }
    int num;
    num=sizeof(vec)-1;
cout<<(num);
  return 0;
}
